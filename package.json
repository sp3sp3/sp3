{
  "dependencies": {
    "@fastify/cors": "^8.4.1",
    "@prisma/client": "^5.6.0",
    "fastify": "^4.24.3",
    "fastify-cli": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.4",
    "prisma": "^5.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "start-server": "npm run build && node ./server/build/server.js",
    "build": "tsc -p ./server/tsconfig.json --outDir ./server/build"
  }
}
