{
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "@trpc/client": "^10.44.1",
    "@trpc/server": "^10.44.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.1",
    "prisma": "^5.6.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "test": "npx prisma generate && npx prisma migrate deploy && NODE_ENV=test jest --verbose --runInBand --coverage --detectOpenHandles"
  }
}
